export type TodoType = {
  id: string;
  title: string;
  description: string;
  completed: boolean;
};
export type sessionType = {
  user: {
    name: string;
    email: string;
    id: string;
    image: string;
  };
};
